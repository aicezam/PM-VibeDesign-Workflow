---
name: "prd-generation"
description: "用户要求创建需求文档，创建PRD文档时，使用此技能生成"
---

# 需求产出工作流

## 触发方式
当用户提供一个初步的想法或需求描述时，请严格按照以下**四个步骤**引导用户。
**🚨 绝对强制指令：你必须一步一步（Step-by-Step）执行！绝不允许一次性输出所有步骤的结果。在每一个带有【等待用户确认】的步骤结束后，你必须停止输出，等待用户的回答！**

---

## 参考文档说明

在执行本工作流时，必须参考以下模板文档（位于 `references/` 目录下）：

| 文档 | 用途 | 说明 |
|-----|------|-----|
| `PRD需求模板(核心要点).md` | 快速同步版 | 面向业务方、管理层，快速同步核心业务目标、范围与主流程 |
| `PRD需求模板(详细).md` | 完整详细版 | 面向产品、设计、研发、测试，包含完整的页面结构、交互流程、业务规则 |
| `PRD可交互原型文档/index.html` | 可交互HTML版 | 基于Tailwind CSS + Mermaid的可交互PRD文档，支持目录导航、流程图渲染 |

**使用原则**：
- 根据用户需求复杂度和受众，选择合适的模板作为基础框架
- 可交互HTML版适合需要在线分享、评审的场景
- 详细版适合需要完整记录开发细节的场景

---

## 步骤一：对话式需求采集与确认

新手往往难以一次性说清需求。AI 应首先采用「灵活对话式」引导，主动评估、温和追问、并进行总结确认。在需求未明确前，**不要急于创建文件或文件夹**。

### 1.1 接收需求
- 接受用户的任意形式输入（一句话、截图、草图、大白话）。
- **不要**要求用户填写复杂的固定模板，降低认知门槛。

### 1.2 核心维度评估与反馈
当你接收到用户的初步需求后，**必须将以下 7 个关键维度的评估结果直接展示给用户**，告诉他们当前需求在哪些维度是清晰的，哪些是缺失的：
1. **背景/痛点**：为什么要做？现状有什么问题？（必须）
2. **业务目标**：做完后想达到什么效果？（必须）
3. **用户与场景**：谁在什么具体情况下使用？（必须）
4. **核心用户旅程**：用户从接触到完成目标的关键路径是怎样的？中间有什么触点和痛点？（必须）
5. **现有方案**：现在他们是怎么解决这个问题的？（重要）
6. **业务规则**：有哪些必须遵守的限制或特定流程？（重要）
7. **参考/竞品**：有没有喜欢的对标产品？（加分项）

### 1.3 深度追问与多轮确认 【等待用户确认】
**🚨 关键规则：不要急于总结，必须进行至少 3 轮深度的启发式追问！**
- **追问方式**：抛弃死板的选项（A/B/C），采用**开放式、启发式**的提问，引导用户多方面、更全面地思考这个需求。
- **追问数量**：每次追问必须在 **3 到 5 个问题**之间。
- **循环机制**：基于用户的上一次回答，挖掘出新的盲点继续追问，至少进行 3 轮对话。
- **最终确认**：只有当你通过多轮追问**完全理解**了需求的所有细节，并且用户明确表示"没有补充了，可以开始写了"之后，你才能进行结构化复述并进入下一步。
- **🛑 强制暂停：在每一轮提问后必须停止输出！等待用户回复后才能进行下一轮动作。**

---

## 步骤二：产出需求文档

需求确认后，直接在当前工作目录下创建项目文件夹，并产出需求文档。

### 2.1 文档命名规范

文档命名格式：`YYMMDD项目名称-文档类型.扩展名`

**命名示例**：
- `260324健身APP-核心PRD.md` — 核心要点版（面向业务方、管理层）
- `260324健身APP-详细PRD.md` — 完整详细版（面向研发团队）
- `index.html` — 可交互HTML版（在线分享评审）

**命名规则说明**：
- `YYMMDD`：六位日期，如 `260324` 表示 2026年3月24日
- `项目名称`：简洁的项目标识，如 `健身APP`、`订单系统`、`用户中心`
- `文档类型`：`核心PRD`、`详细PRD`、`可交互PRD`

### 2.2 目录结构

```
项目名称/
├── YYMMDD项目名称-核心PRD.md      # 核心要点版
├── YYMMDD项目名称-详细PRD.md      # 完整详细版
├── index.html  # 可交互HTML版
└── static/                        # 静态资源（HTML依赖）
    ├── tailwindcss.js
    └── mermaid.min.js
```

### 2.3 产出内容

根据步骤一确认的需求信息，参考模板文档生成以下文件：

**核心PRD文档**（参考 `PRD需求模板(核心要点).md`）：
1. 业务背景与目标
2. 目标用户与核心场景
3. 产品范围与功能清单
4. 核心业务主流程
5. 核心页面与功能说明
6. FAQ
7. 后续规划

**详细PRD文档**（参考 `PRD需求模板(详细).md`）：
1. 文档概述（含修订历史）
2. 产品目标与背景
3. 用户与使用场景
4. 功能清单
5. 功能流程图（Mermaid）
6. 页面说明与交互细节
7. 数据需求
8. 非功能性需求
9. 依赖与风险
10. 验收标准
11. 后续规划
12. 附录

### 2.4 用户确认 【等待用户确认】
询问用户："需求文档已生成，包括核心版和详细版。您看方向和业务逻辑准确吗？如果没问题，我们将生成可交互的HTML版本文档。"
**🛑 强制暂停：在此处必须停止输出！等待用户回复同意后，才能进入步骤三。**

---

## 步骤三：产出可交互式PRD HTML文档

基于步骤二确认的需求文档内容，生成一份可交互的HTML格式PRD文档，方便在线分享和团队评审。

### 3.1 HTML文档规范
1. 参考 `PRD可交互原型文档/index.html` 的结构和样式
2. **技术栈要求**：
   - 使用 **Tailwind CSS** 进行样式设计
   - 使用 **Mermaid.js** 渲染流程图
   - 采用响应式布局，支持移动端和桌面端
3. **交互功能**：
   - 左侧固定导航目录，支持快速跳转
   - 移动端支持侧边栏收起/展开
   - 滚动时自动高亮当前章节
   - 表格支持水平滚动
   - 复选框支持交互勾选

### 3.2 内容同步
- HTML文档的内容必须与详细版Markdown文档保持一致
- 所有流程图使用Mermaid语法内嵌，确保可渲染
- 表格数据保持格式统一

### 3.3 用户确认 【等待用户确认】
询问用户："可交互的HTML文档已生成，您可以在浏览器中打开查看。是否需要对内容进行调整？"
**🛑 强制暂停：在此处必须停止输出！等待用户反馈修改意见。**

---

## 步骤四：版本迭代与管理

当项目进入后续迭代阶段时，通过更新文档内的版本号和修订历史来管理版本，保持文件命名不变。

### 4.1 版本控制方式
- **不创建新文件**：在同一文件中更新内容
- **更新修订历史**：在文档开头的修订历史表格中新增版本记录
- **更新版本号**：修改文档概述中的版本字段（如 v1.0 → v1.1）

### 4.2 修订历史格式

| 版本 | 日期 | 作者 | 变更说明 |
|-----|------|-----|---------|
| v1.0 | 260324 | 作者名 | 初始版本 |
| v1.1 | 260328 | 作者名 | 新增XX功能，优化XX流程 |

### 4.3 变更内容标注
- 建议对变更内容进行标注（如使用高亮或备注），方便团队成员快速识别变更点
- HTML版本可在变更处添加醒目的样式标记

---

## 交付物清单

完成本工作流后，将产出以下文档：

| 交付物 | 格式 | 文件名示例 | 用途 |
|-------|------|-----------|------|
| 核心PRD | Markdown | `260324健身APP-核心PRD.md` | 业务方、管理层快速同步 |
| 详细PRD | Markdown | `260324健身APP-详细PRD.md` | 研发团队开发参考 |
| 可交互PRD | HTML | `index.html` | 在线分享、团队评审 |

---

## 快速参考：核心维度检查表

在需求采集阶段，可使用以下检查表快速评估需求完整度：

| 维度 | 检查项 | 状态 |
|-----|-------|------|
| 背景/痛点 | 是否清晰说明了为什么要做？ | ✅/❌ |
| 业务目标 | 是否有可量化的目标指标？ | ✅/❌ |
| 用户与场景 | 是否明确了目标用户和使用场景？ | ✅/❌ |
| 核心用户旅程 | 是否描述了用户完成目标的关键路径？ | ✅/❌ |
| 现有方案 | 是否了解了当前的解决方案？ | ✅/❌ |
| 业务规则 | 是否明确了限制条件和特定流程？ | ✅/❌ |
| 参考/竞品 | 是否有对标产品可参考？ | ✅/❌ |
